package com.example.albam.domain.store.entity;

/**
 * 매장 요금제.
 *
 * <p>아직 어떤 기능도 이 값을 보지 않는다. 유료화를 붙일 때 매장마다 등급을 저장할 자리가 필요해
 * 먼저 만들어 둔 것이고, 그때 기능 제한이나 결제 연동이 이 값을 읽게 된다.
 *
 * <p>이름을 DB에 문자열로 저장하므로 순서를 바꿔도 안전하지만, 이미 저장된 이름을 바꾸면 기존 행을
 * 읽을 수 없게 된다. 등급을 늘릴 때는 새 이름을 추가한다.
 */
public enum StorePlan {

    /** 기본값. 지금은 모든 매장이 여기에 속한다. */
    BASIC,
    PREMIUM
}
