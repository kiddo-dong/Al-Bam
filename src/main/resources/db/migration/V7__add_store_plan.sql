-- 매장 요금제. 유료화를 붙일 때 등급을 저장할 자리를 먼저 만들어 둔다.
--
-- 아직 어떤 기능도 이 값을 읽지 않는다. 지금 넣어두는 이유는, 나중에 필요해졌을 때 이미 매장이
-- 쌓인 상태에서 컬럼을 채우는 것보다 비어 있을 때 기본값으로 맞춰두는 편이 간단하기 때문이다.
--
-- 값을 바꾸는 경로는 일부러 만들지 않았다. 요청 본문으로 등급을 올릴 수 있으면 결제를 건너뛰는
-- 길이 되므로, 결제를 붙일 때 그쪽에서만 바뀌도록 한다.

ALTER TABLE stores ADD COLUMN plan VARCHAR(20) NOT NULL DEFAULT 'BASIC';
