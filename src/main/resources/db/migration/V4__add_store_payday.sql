-- 급여 지급일(매월 며칠).
--
-- 알바생이 "언제 급여를 받는지" 앱에서 볼 수 있게 하기 위한 값이다. 앱이 지급을 처리하지는 않으므로
-- 31일로 둔 매장의 2월을 며칠로 볼지 같은 규칙은 두지 않는다 — 실제 지급은 사장이 알아서 한다.
--
-- 기존 매장은 정해둔 값이 없으므로 NULL(미지정)로 시작한다.

ALTER TABLE stores ADD COLUMN payday INT NULL;
