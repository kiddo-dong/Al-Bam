-- 가입 신청에 낙관적 락 버전을 둔다.
--
-- 매니저 둘이 같은 신청을 거의 동시에 승인과 거절로 처리하면, 둘 다 "대기 중"을 보고 통과한다. 늦게
-- 저장한 쪽이 이겨서, 상태는 "거절"인데 멤버는 이미 만들어져 있는 식으로 서로 어긋난 결과가 남았다.
-- 같은 행을 두 사람이 고치는 경우라 버전 비교로 늦은 쪽을 실패시킨다. 충돌 자체가 드물고 진 쪽은
-- 다시 시도할 필요도 없어, 행을 미리 잠그는 비관적 락까지 쓸 일은 아니다.

ALTER TABLE join_requests ADD COLUMN version BIGINT NOT NULL DEFAULT 0;
